/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as environments from "../../../../environments.js";
import * as core from "../../../../core/index.js";
import * as AiInfraApi from "../../../index.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../core/headers.js";
import * as errors from "../../../../errors/index.js";

export declare namespace CodeInterpreter {
    export interface Options {
        environment?: core.Supplier<environments.AiInfraApiEnvironment | string>;
        /** Specify a custom URL to connect the client to. */
        baseUrl?: core.Supplier<string>;
        token?: core.Supplier<core.BearerToken | undefined>;
        /** Additional headers to include in requests. */
        headers?: Record<string, string | core.Supplier<string | undefined> | undefined>;
    }

    export interface RequestOptions {
        /** The maximum time to wait for a response in seconds. */
        timeoutInSeconds?: number;
        /** The number of times to retry the request. Defaults to 2. */
        maxRetries?: number;
        /** A hook to abort the request. */
        abortSignal?: AbortSignal;
        /** Additional query string parameters to include in the request. */
        queryParams?: Record<string, unknown>;
        /** Additional headers to include in the request. */
        headers?: Record<string, string | core.Supplier<string | undefined> | undefined>;
    }
}

export class CodeInterpreter {
    protected readonly _options: CodeInterpreter.Options;

    constructor(_options: CodeInterpreter.Options = {}) {
        this._options = _options;
    }

    /**
     * Create a new code interpreter sandbox for executing Python code.
     *
     * @param {AiInfraApi.CreateSandboxRequest} request
     * @param {CodeInterpreter.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link AiInfraApi.UnprocessableEntityError}
     *
     * @example
     *     await client.codeInterpreter.create({})
     */
    public create(
        request: AiInfraApi.CreateSandboxRequest,
        requestOptions?: CodeInterpreter.RequestOptions,
    ): core.HttpResponsePromise<AiInfraApi.SandboxResponse> {
        return core.HttpResponsePromise.fromPromise(this.__create(request, requestOptions));
    }

    private async __create(
        request: AiInfraApi.CreateSandboxRequest,
        requestOptions?: CodeInterpreter.RequestOptions,
    ): Promise<core.WithRawResponse<AiInfraApi.SandboxResponse>> {
        let _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({ Authorization: await this._getAuthorizationHeader() }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.AiInfraApiEnvironment.Production,
                "sandboxes/code-interpreter",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryParameters: requestOptions?.queryParams,
            requestType: "json",
            body: request,
            timeoutMs: requestOptions?.timeoutInSeconds != null ? requestOptions.timeoutInSeconds * 1000 : 60000,
            maxRetries: requestOptions?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
        });
        if (_response.ok) {
            return { data: _response.body as AiInfraApi.SandboxResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new AiInfraApi.UnprocessableEntityError(
                        _response.error.body as AiInfraApi.HttpValidationError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.AiInfraApiError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.AiInfraApiError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.AiInfraApiTimeoutError(
                    "Timeout exceeded when calling POST /sandboxes/code-interpreter.",
                );
            case "unknown":
                throw new errors.AiInfraApiError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    protected async _getAuthorizationHeader(): Promise<string | undefined> {
        const bearer = await core.Supplier.get(this._options.token);
        if (bearer != null) {
            return `Bearer ${bearer}`;
        }

        return undefined;
    }
}
